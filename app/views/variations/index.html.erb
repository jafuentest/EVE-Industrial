<h1>Listing Variations</h1>

<table style="width: auto;" class="table table-bordered table-striped">
  <tr>
    <th>Name</th>
    <th>Bonus %</th>
    <th class="edit-field">Edit</th>
  </tr>

<% @variations.each do |variation| %>
  <tr>
    <td><%= link_to variation.name, variation %></td>
    <td><%= variation.bonus %></td>
    <td class="edit-field">
      <%= link_to image_tag("/edit.png", :alt => "Edit"), edit_variation_path(variation) %>
      <%= link_to image_tag("/delete.png", :alt => "Delete"), variation, method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
  </tr>
<% end %>
</table>

<br />

<a class="btn btn-primary" href="<%= new_variation_path %>" role="button">New Mineral »</a>
